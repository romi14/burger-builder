//You can never create a new file named 'Aux' because it is a reserved keyword in windows OS in the ZIP archives

//This is basically an empty or technical wrapper that uses the special property children to print what is wrapped inside it

//import React from 'react';

const aux = props => props.children;

export default aux;